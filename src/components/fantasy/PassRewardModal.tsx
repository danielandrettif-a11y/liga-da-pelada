"use client";

// Backwards-compatible alias. Any older route/import now receives the same
// picker with the explicit “Escolha já realizada” state.
export { PassRewardPicker as PassRewardModal } from "./PassRewardPicker";
