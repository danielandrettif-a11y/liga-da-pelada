import { ManagerDemo } from "@/components/bq-manager/ManagerExperience";

export const metadata = { title: "Demonstração BQ Manager", robots: { index: false, follow: false } };

export default function ManagerDemoPage() {
  return <ManagerDemo />;
}
